## String

```js
const word = '문자';
const word2 = "문자";
const word3 = `문자`;
const words = word + ' ' + word2

console.log(word === word3); // true
console.log(words); // 문자 문자
console.log(words.length); // 5
```